console.log('app.js is running');
/*
var arr=['Name', 'Place', 'Animal', 'Thing'];

var narr = arr.map(function (item, index) {
    return (
        <div>
            <span>Sai from the outer world</span>
            <span>Siddharth Lanka {item}, {index+1}</span>
        </div>
    );
});
*/
// render subtitle only if it exists
// render new p tag only if options.length > 0 "Here are your options" "No options"
